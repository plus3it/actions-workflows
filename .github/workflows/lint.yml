name: Run lint and static analyis checks
on:
  workflow_call:
    inputs:
      tardigradelint-target:
        description: Name of the Tardigrade-CI Makefile target to run
        default: lint
        required: false
        type: string

jobs:
  tardigradelint:
    runs-on: ubuntu-latest
    steps:
      - name: Clone this git repository
        uses: actions/checkout@d632683dd7b4114ad314bca15554477dd762a938

      - name: Project Syntax Verification
        run: make docker/run target="${{ inputs.tardigradelint-target }}"

  actionlint:
    runs-on: ubuntu-latest
    steps:
      - name: Clone this git repository
        uses: actions/checkout@d632683dd7b4114ad314bca15554477dd762a938

      - name: Check workflow files
        uses: reviewdog/action-actionlint@05c9d7bef25a46caf572df3497afa7082fc111df
